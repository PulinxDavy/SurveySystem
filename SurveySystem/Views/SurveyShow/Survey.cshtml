@using System.Runtime.InteropServices
@using SurveySystem.Models;

@model SurveySystem.ViewModels.SurveyShowVM

@{
    ViewBag.Title = "Survey";
}

<h2>Survey</h2>

<div>
    <h4>@Html.DisplayFor(model => model.Survey.Title)</h4>
    <hr/>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Survey.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Survey.Description)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Start", "Group", new { id = Model.Survey.Id })
</p>
